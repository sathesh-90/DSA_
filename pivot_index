class Solution(object):
    def pivotIndex(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        s = sum(nums)
        l = 0
        for i , n in enumerate(nums):
            if l == s-l-n:
                return i
            l+=n
        return -1        
        
